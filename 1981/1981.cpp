
#include <bits/stdc++.h>


using namespace std;


#define REP(i,a,b) for(int i = a ; i < b ; i ++)

typedef vector<int> vi;
typedef long long ll;


int maxN = 0 ;
int minN = INT_MAX;

int dynamic()
{
	int dp[n][n] ; 
	memset(dp,0,sizeof(dp));
	REP(i,0,n) dp[0][i] = board[0][i];
	
	REP(i,1,n){
		REP(j,0,n){
			if
		}
	}
}

int mai(){
	sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int n = 0;
	cin >> n;
	int board[n][n];
	REP(i,0,n){
		REP(j,0,n){
			cin >> baord[i][j];
		}
	}

}
